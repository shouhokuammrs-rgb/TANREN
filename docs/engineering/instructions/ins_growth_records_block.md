# INS: 成長タブ「トレーニング量」ブロック(ISS-022)

- 発行: PM / 2026-08-01 / 対象: Engineer / 運用: DEC-014準拠

## 仕様
- 成長タブ「成長」セグメントの最下部(e1RM推移カード群の後)に「トレーニング量」ブロックを新設し、IA再設計で撤去された3点を**移設のみ**で復帰: ①部位別セット数グラフ(ISS-012の週/日切替そのまま) ②統計カード(連続記録・今週ボリューム) ③体重推移グラフ
- 見出しは既存キッカー流儀(Mono 10px)。コンポーネント・データは残置済みのため新規実装なし。期間セグメント(30日/90日)との連動は**しない**(各グラフは従来の期間仕様のまま。連動させたくなったらISS)
- 写真セグメント側には出さない

## テスト
移設後の描画+既存グラフテスト回帰なし。E2Eで成長セグメント末尾に3点表示

## 完了条件
handoff(既存IA handoffへの追記可)+SSOT(ISS-022対応済み)+main push
