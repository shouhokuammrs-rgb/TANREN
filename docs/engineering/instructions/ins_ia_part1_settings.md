# INS: IA再設計 前半 — 設定3群+共通適用(DEC-015 / Phase 8-5a)

- 発行: PM / 2026-08-01 / 対象: Engineer / 運用: DEC-014準拠
- 視覚仕様の正: `docs/design/handoff/6b_ia_handoff.md` §4・§5・§6(PM裁定反映済み・最新をpull)。本INSはエンジニアリング差分のみ

## スコープ
1. **旧ゴールセクション撤去**: UI・状態・保存処理ごと削除(P-1。旧want/avoidタグはエンジンで読み捨て済みのためデータ移行不要。読み捨てコード自体も削除してよいが、selection.tsのインターフェースは変えない)
2. **設定3群化**(§4-1〜4-3): ①日常(ゴールゲージ+体重)②からだと器具(アコーディオン既定閉・一度に1項目・閉時52px+現在値要約)③データ。既存セクションの中身は再利用し、配置と外殻のみ変更
3. **全削除の隔離**(§4-4): `/settings/danger` 新設。テキスト入力「削除」一致で有効化→ネイティブアラート2段確認→実行前に「先にエクスポートする」導線。#D8321Aはこの画面限定
4. **サマリー→ログ編集直行導線**(ISS-020追記分): サマリーに当該セッションのログ編集への1リンク
5. **体重保存の履歴化**(PM裁定): 体重保存(設定①/後半の成長タブチップ共通)が body_stats へ1レコード追記になっていることを確認・必要なら修正

## テスト
danger画面の有効化ガード(入力不一致で実行不可)/ 3群の描画 / 旧ゴール削除後の設定保存回帰 / サマリー→編集遷移

## 完了条件
handoff+SSOT(WBS 8-5a✔)+main push。迷いはPM確認事項へ
