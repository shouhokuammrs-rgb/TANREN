# Instruction: 定常運転 #3改訂 — バックアップ補完(クラウド前提版)

- Priority: 🟡(Phase 5マージ後に着手。旧ops3=2026-07-17_ops3_backup_hardening.mdは本書で置換・旧書は対応不要)
- 発行: PM / 2026-07-18
- 対応Issue: ISS-014(縮小改訂)

## 方針

Phase 5のクラウド自動バックアップが主防衛線になったため、旧ops3の手動催促系(3セッション強調等)は廃案。
残すのは「未ログイン者への導線」と「危険操作の警告」のみ。

## 要件

1. **未ログイン時のみ**: ホームのリマインダーを「☁️ クラウドバックアップ未設定です。アイコン削除や機種変で記録が消えます → 設定へ」に差し替え(ログイン済みなら非表示)。既存の7日エクスポートリマインダーはログイン済みユーザーには表示しない
2. **警告明示**: 設定データ管理と全データ削除ダイアログに「⚠️ ホーム画面のアイコン削除で端末内の記録は消えます(iOS仕様)。クラウドログイン済みなら復元可能です」を追記
3. トレ終了サマリー末尾: ログイン済み=「☁️ 同期済み HH:MM」のさりげない表示 / 未ログイン=「💾 バックアップする」ボタン(既存エクスポート起動)

## 受け入れ条件

- [ ] ログイン状態で催促が消え、未ログインで導線が出る(両状態E2E)
- [ ] テストgreen、mainへのPR作成まで。state.mdのISS-014を本改訂内容でクローズ
