# INS: ゴールプリセット第4段「ひかえめ」(DEC-016)

- 発行: PM / 2026-08-01 / 対象: Engineer / 運用: DEC-014準拠
- 仕様の正: `docs/design/handoff/dec016_hikaeme_diff_spec.md`(PM裁定反映済み・§5採用)。本INSは要点のみ

## スコープ
1. `GoalLevel` に `'light'` 追加(light→toned→solid→big連鎖・マイグレーション不要)。`GOAL_COEF` にひかえめ列: 胸0.20/背中0.20/肩0.15/腕0.10/脚0.15
2. 5bゲージのノッチ4個化: X座標正規化・寸法は既存のまま。**選択中ノッチをz-order最前面に**。タップ領域44px拡張はしない
3. ひかえめの生活言語キャプション5部位(仕様§3の表の文言そのまま)
4. **保存時即到達(§5採用)**: `saveMuscleGoal` で現在≧新目標かつreachedAt未設定なら reachedAt設定+`goal_events` に `reached` 記録(重複抑止は既存フロー)
5. 鏡チェック「物足りない」の1段上げ連鎖が light→toned に伸びることを確認(コード変更は連鎖定義のみのはず)

## テスト
仕様§6のテスト観点どおり(4段連鎖/係数計算/腕42.3kg境界の座標/保存時即到達のイベント+重複抑止)+E2E: 390pxで腕4ノッチ・ひかえめ選択→保存→即状態4→鏡チェック「満足」→維持、の1画面完結フロー

## 完了条件
handoff+SSOT(WBS定常#7としてDEC-016記載・対応済み)+main push
