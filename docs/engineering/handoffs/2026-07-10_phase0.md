# Handoff: Phase 0 — プロジェクト基盤構築+TANREN改名

- 日付: 2026-07-10
- 担当: Engineer(Claude Code)
- 対応指示書: `docs/engineering/instructions/2026-07-10_phase0_scaffold.md`
- ブランチ: `claude/tetsu-tanren-rename-p25kvo`

## 【Eiichiアクション】

1. **Vercelでリポジトリをインポートしてデプロイ(約10分)**
   - https://vercel.com/new → GitHubの `TANREN` リポジトリを選択 → Framework Presetは「Vite」が自動検出されるのでそのままDeploy
   - 設定変更は不要(ビルドコマンド `npm run build`・出力 `dist` が自動適用。SPA用の `vercel.json` は配置済み)
2. **デプロイ後、iPhone Safariで発行URLを開き「ホーム画面に追加」**(約2分)
   - standalone(アドレスバーなし)で起動し、下部に4タブが表示されればOK
3. このブランチのPRレビュー&マージ(またはそのままマージでOK)

## やったこと

### 1. プロジェクト名を TANREN(鍛錬)に正式決定・全置換(DEC-001)

- `docs/pm/tetsu_project_state.md` → `tanren_project_state.md` にリネーム
- `docs/skills/tetsu-pm/` → `tanren-pm/` にリネーム(スキル名も `tanren-pm` に変更)
- CLAUDE.md・要件定義書・指示書・SKILL.md内のTETSU表記を全置換
- state.md意思決定記録にDEC-001を追加、WBS 4-3(正式名称決定)を前倒し完了に
- ⚠️ **PMへ**: Web/App版Claudeのスキルとして旧 `tetsu-pm` を登録している場合は、`docs/skills/tanren-pm/SKILL.md` で再登録が必要

### 2. Phase 0 scaffold(指示書要件1〜6すべて対応)

- **Vite 8 + React 19 + TypeScript 6 + Tailwind CSS 4**(公式最新テンプレートベース、oxlint付き)
- **PWA**: vite-plugin-pwa。manifest名称 `TANREN`・standalone・ja・テーマカラー仮 `#0f172a`。仮アイコン(ダンベルピクト192/512/apple-touch-icon)を生成して配置(Phase 4で正式デザインに差し替え)
- **Dexie.js スキーマv1**(`src/db/db.ts`): 要件§7の10テーブル(profiles / goals / equipment / exercises / sessions / session_exercises / sets / photos / body_stats / injuries)。型定義は `src/db/types.ts`
- **器具初期シード**(`src/constants/equipment.ts`、初回DB作成時に自動投入):
  - 可変ダンベル 2.5〜24kg・15段階 ×2 — **重量ステップ配列は仮値**(DEC-002待ち。配列を差し替えるだけで対応可能)
  - アジャスタブルベンチ -20°〜90° / 自重・床
- **エンジン雛形**(`src/engine/`、UI非依存の純関数):
  - `freshness.ts` 回復モデル(線形・大筋群72h/小筋群48h)
  - `interval.ts` インターバルテーブル適用(180/90/60秒+コンパウンド+30秒)
  - `menu.ts` メニュー生成のI/F定義のみ(実装はPhase 1 WBS 1-3)
  - 回復係数・インターバルは `src/constants/recovery.ts` / `intervals.ts` に分離済み(係数調整はこのファイルだけ触ればよい)
- **4タブナビ**: ホーム / 今日のトレ / ログ / 設定(react-router-dom+下部固定タブ、タップターゲット64px、safe-area対応)。中身はプレースホルダー、設定タブのみ器具一覧を実表示
- **UI文言**は `src/constants/copy.ts` に集約
- `vercel.json`(SPAリライト)・`README.md` 追加

## 受け入れ条件の検証結果

- [x] `npm run dev`(previewで検証)でモバイル幅390pxに4タブ表示 — ヘッドレスChromium実寸スクリーンショットで確認
- [x] `npm run test` green — **2ファイル11テストすべてpass**(回復モデル+インターバル)
- [x] standalone起動 — manifestが `display: standalone`・アイコン配信を確認済み。**実機確認はEiichiアクション2**
- [x] Dexieシード投入+設定タブに器具一覧表示 — 「可変ダンベル×2 2.5〜24kg・15段階 / ベンチ -20°〜90° / 自重・床」の表示を確認
- 追加検証: `npm run build`(tsc型チェック込み)成功・oxlintクリーン

## 技術メモ・判断記録

- **Tailwind v4**(v3ではなく)を採用: 現行安定版。`@tailwindcss/vite` プラグイン方式でtailwind.config不要
- **Recharts / fake-indexeddbは未使用**: Rechartsは Phase 2(グラフ)で導入。fake-indexeddbはDB層テスト用にdevDependenciesへ先行投入済み
- **isActiveフラグは `0 | 1`**: IndexedDBはbooleanをインデックスできないため
- ダンベルシードは1レコード+`quantity: 2`+`weightStepsKg`配列で表現(エンジンはこの配列にスナップする設計)

## 次のアクション

- Eiichi: 上記アクション1〜2(Vercelデプロイ+実機確認)→ 完了でWBS 0-4が閉じる
- PM: Phase 1指示書の作成(1-1 種目マスタ35種目から)
