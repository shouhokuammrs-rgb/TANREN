# Handoff: Phase 4 Part 2 — デザイン磨き込み(炉心/スパークバースト)

- 日付: 2026-07-10
- 担当: Engineer(Claude Code)
- 対応指示書: `docs/engineering/instructions/2026-07-10_phase4_polish.md` Part 2
- 正: `docs/design/specs/2026-07-10_design_spec_v1.md`(§0 PM補記を最優先で適用)
- ブランチ: `claude/tetsu-tanren-rename-p25kvo`(PR #6にPart 1と合わせて提出)

## 【Eiichiアクション】

1. **PR #6をマージ**(Part 1のデータ保全と本デザインが一緒に入ります)
2. **実機レビュー=ドッグフーディング#3**: 見た目の最終判断はEiichi。特に「1m離れてタイマーが読めるか」「LOGGINGの±が汗した手で押せるか」「PR演出が嬉しいか」
3. ホーム画面にアイコンを再追加すると新アイコン(スパークバースト)になります(古いものを削除→再追加)

## 実装サマリー

### トークン整備(作業指針1・受け入れ条件「直書きなし」)

- `src/index.css` の `@theme` を仕様書§1〜3の正式値に差し替え: 炉心12色+派生3色(カード地/区切り/タブ非アクティブ)+セマンティック4色(§0-4でステータス専用として併存)+角丸4段+書体3役
- 共通コンポーネントクラス: `.card-ember` `.pill-molten` `.pill-ghost` `.num-hero` `.label-mono` `.glow-text` `.pr-gradient-text`
- **全17ファイルのslate/orange直書きを一掃**(grepで残存ゼロを確認)。チャート・人体図の実値も炉心系に統一
- フォントは§0-5どおり**セルフホスト**(@fontsource: Anton / IBM Plex Mono 500・700 / Noto Sans JP 400・700・900)。Noto Sans JPはunicode-rangeサブセット構成のため、**SWはフォントをプリキャッシュせず実行時CacheFirst**(使ったサブセットだけがキャッシュされ、オフライン動作と容量配慮を両立)

### 実行画面(§4+§0の統合ルール)

- **RESTING / LOGGINGのステートマシンに再構築**。riseIn遷移(.35s)、NOWラベルとリングにemberPulse
- RESTING: 300pxリング(溶鉄グラデ45°・round cap・グロー・1s linearの`stroke-dashoffset`)、中央にINTERVAL+残秒(Anton 88px)+`/設定秒`。**±15秒+スキップ**(§0-2: +30秒は不採用、見た目だけ流用)。sec==0でチャイム+バイブ+点滅→LOGGINGへ自動遷移
- LOGGING: WEIGHT/KG・REPSカード(Anton 64px+グロー、56px円形±)。**重量±は器具の重量配列にスナップ**(§0-3)、レップ±1。**「記録」+「🔥限界でした」+達成緑/調整中黄フィードバックを維持**(§0-4)
- セット数は実セット数で一般化(§0-1)、種目間もRESTING、全体最終セットのみタイマーなしでサマリーへ(ISS-006維持)
- ISS-005の音復帰(タップごとresume+🔇フォールバック表示)・Wake Lock・中断保存・メモ(折りたたみ)・種目詳細シート(種目名タップ)をすべて維持

### ホーム/サマリー/PR演出(§5)

- ホーム: TANRENワードマーク+日付 → 挨拶見出し(900/30px・2行)→ **統計カード×2(連続記録・今週ボリューム、Anton 40px+グロー)** → moltenピルCTA → 既存ウィジェット(チャート/人体図/体重)をトークン化して継続
- サマリー: 日付+所要(Mono)→「ワークアウト完了。」→ **総ボリュームヒーロー(Anton 84px+グロー)** → PRカード → 種目リスト(line-soft区切り・前回比つき)
- **PR演出**: molten枠+40pxグロー、PRグラデtext-clip数字+**heatShimmer(ここ以外未使用)**、ヒートゲージ14セグメント(gold→molten→deep-red・各セグメントグロー)。CSSのみ・常時2.8sループ(3秒以内の規定は1周期で充足)・**prefers-reduced-motionで全アニメ停止**
- タブバー: アクティブ=text-hot+moltenドット、非アクティブ=#6B5A4C(ラベルは既存4つを維持=§0-6)

### アプリアイコン(指針4)

- `docs/design/assets/tanren-icon-sparkburst.svg` を正としてリポジトリに配置(`public/icons/` にも同梱)し、**192/512/apple-touch(180) PNGを生成して差し替え**。manifest/`theme-color` を `#0B0907` に更新

## 受け入れ条件の検証結果

- [x] トークン定数化完了・直書きなし — grepでslate/orange/rounded-xl等の残存ゼロ
- [x] 全画面スクリーンショット(390px)— 本handoffと同送(home / hearing / menu / workout-logging / workout-resting / summary-pr / log / log-detail / setup / photos / settings / detail-sheet)。PMレビュー対象
- [x] 既存テストgreen — 12ファイル**96本**(ロジック無変更)。build/lintクリーン
- [x] E2E: LOGGING→記録→RESTING→スキップ→次セット→最終セット→サマリー、2セッション目でPRカード表示を確認
- [x] PR作成 — PR #6をPart 1+2に更新

## 方針書との整合(自己評価)

| 原則 | 評価 |
|------|------|
| トレ中は片手・汗・低思考 | ◎ 実行画面は常に「今の1セット」だけ。±56px円形・CTA56px高 |
| 数字はヒーロー | ◎ Anton+tabular-nums(タイマー88/ボリューム84/入力64/統計40/NEXT34) |
| 暗がりに炎 | ◎ moltenはCTA・進捗・強調のみ。goldはPR演出限定 |
| 未達は失敗ではない | ◎ 緑/黄セマンティックを維持(§0-4) |
| 祝う時は思い切り祝う | ○ PRカード(グラデ数字+shimmer+ヒートゲージ)。火花パーティクルまでは踏み込まず=改善余地 |

## 決定事項(PMレビュー対象)

1. ホームの「今日のメニューカード」(§5モック)は未実装 — 現仕様はヒヤリング後に生成するため、生成前のメニュー表示は仕様と矛盾。実装するなら「前回の続き/生成済みメニューの再掲」の仕様化が必要(提案として保留)
2. サマリーの「記録を保存する/共有」ボタン(§5原文)→ 記録は保存済みのため「ログを見る/ホームへ」を維持(§0の一般化ルール準拠)。スクショ共有ニーズがあれば次ISSで
3. フォントのSW戦略: プリキャッシュ除外+実行時CacheFirst(理由は上記)
4. 統計カードの「今週ボリューム」単位はkg(仕様の(t)は58kg×ダンベルの実態だと0.x tになり迫力が出ないため)
5. 旧TimerOverlay/Stepperコンポーネントは削除(RESTING/HeroStepperに置換)

## 次のアクション

- Eiichi: PR #6マージ→実機レビュー(ドッグフーディング#3)→アイコン再追加
- PM: スクリーンショット12枚のレビュー、決定事項1〜5(特に1のメニューカード扱い)の判断
