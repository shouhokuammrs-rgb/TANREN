# Handoff: 旧ゴール後始末(ISS-023)+PWA更新トースト(ISS-025)+ひかえめ(DEC-016)

- 日付: 2026-08-01
- 担当: Engineer(Claude Code)
- 対応指示書: `ins_legacy_goal_cleanup.md` / `ins_pwa_update_toast.md`(追記反映)/ `ins_hikaeme_level.md`
- 仕様の正(DEC-016): `docs/design/handoff/dec016_hikaeme_diff_spec.md`(PM裁定反映済み)
- 反映先: main直接push(DEC-014準拠・コミットは件ごと)

【Eiichiアクション】1件・約1分: **ISS-025の実機確認**。次回デプロイ後にアプリを開き→さらに次のデプロイが出た状態で再度開くと、画面下(タブバー上)に「更新があります — 再読み込み」が出ます。タップで新版に切り替わることを確認してください(トレ実行中・サマリー表示中は出ません=正常)。

## ISS-023: 旧ゴール残骸の後始末

- ホームのセットアップバナー判定を`muscle_goals` 0件基準へ(旧`goal === null`誤発火の解消)。文言を部位別ゴールに差し替え、CTAは`/settings`(①日常)へ。鈍色枠#3A2213で鏡チェックバナー(#FFB300)と区別
- セットアップウィザードから目標ボディステップ(goalType/focusParts)・旧ヒヤリング(wantParts/avoidParts)・ギャップ分析結果を撤去。**profile→photos→怪我**の3ステップ+末尾1行「部位別ゴールは、設定からいつでも決められます。」(Noto 400 12px #8A5A3C)
- 読み取り残骸を削除: `loadGoal`/`saveGoal`・`priorityScores(旧)`/`analyzeGap`・旧F-03定数(GOAL_PRIORITY_MAP/WANT_BOOST/AVOID_FACTOR等)・GAP_COPY等。**goalsテーブルのDBレコードとスキーマは残置**(バックアップ後方互換維持・INSどおり)
- E2E 7チェック: 0件表示→CTA遷移/目標ステップ不在/旧ヒヤリング不在/末尾案内/完了でホーム直行/1件以上で非表示

## ISS-025: PWA更新トースト

- `vite-plugin-pwa`を`registerType: 'prompt'`へ変更し、標準`registerSW`の`onNeedRefresh`で新SW待機を検知(自作SWロジックなし)。タップで`skipWaiting`→リロード
- 視覚仕様(Designer指定)どおり: タブバー上に浮かせる・#1A110B/#3A2213の鈍色・molten不使用・✕(44pxターゲット)で次回起動まで非表示(メモリ保持=次回起動時に再表示)
- 表示条件は純関数`updateToastState`(`utils/updateToast.ts`): **`/workout/active`と`/summary`では完全非表示に保留**し、保留後は途中画面(ログ等)を挟んでも**ホーム復帰時に表示**(PM裁定追記どおり)
- ユニット6本(即表示/active保留/summary保留/保留→ログ経由→ホーム復帰で表示/成長タブ即表示/dismissed)
- **実機確認手順**: ①現版をPWAで開く ②新デプロイ後にアプリを再フォアグラウンド(またはタブ再訪)→SW待機検知でトースト表示 ③タップ→リロードで新版 ④トレ実行中に②を行った場合は出ず、終了してホームに戻ると表示

## DEC-016: ゴール第4段「ひかえめ」

- `GoalLevel`に`'light'`追加(連鎖 light→toned→solid→big・既存保存値は無変更でマイグレーション不要)。係数=Strength Level Beginner: 胸0.20/背中0.20/肩0.15/腕0.10/脚0.15(全体重域40–120kgでcappedなし)
- ゲージ4ノッチ化: X座標正規化・18px寸法は既存のまま。選択中ノッチをz-order最前面に(低体重で腕の第1・第2が接近しても点灯が隠れない)。タップ領域44px拡張はしない(仕様§2-3裁定)。腕の間隔境界42.3kg=18pxはテストで固定
- 生活言語キャプション5部位(仕様§3の文言そのまま・機能語)
- **保存時即到達(§5・PM裁定採用)**: `saveMuscleGoal`で「現在e1RM ≧ 新目標」かつ`reachedAt`未設定(growthのみ)なら即`reachedAt`+`goal_events`に`reached`記録。既超過部位にひかえめを設定→保存した瞬間から抑制運転(即状態4→鏡チェック→維持)が始まる。ISS-019のクリア規則と共存: 目標引き上げでクリア(無イベント)・下げ直すと新目標への正規reached
- E2E(390px)11チェック: 腕4ノッチ→胸ひかえめ(0.20表示)→保存→**即「達成 — 判定待ち」**→行タップ→鏡チェック「満足」→**✓維持まで設定1画面完結**・`goal_events=[reached, maintain]`

## テスト(全245本green・tsc/oxlint/本番ビルドクリーン)

件ごとの内訳は各コミットメッセージ参照(ISS-023: E2E 7 / ISS-025: ユニット6 / DEC-016: ユニット10+E2E 11)。既存テストの更新1本: ISS-019「クリア後は保存で復活しない」→ DEC-016裁定により「下げ直すと保存時に正規reachedで再到達」へ仕様変更を反映

## PM確認事項

1. **ISS-019リグレッションテストの更新**: 上記のとおり、DEC-016 §5採用により旧アサーション(保存では復活しない)が新仕様(即到達で復活する)と矛盾したため、テストを新裁定側に書き換えました。裁定文の帰結どおりの挙動ですが、明示的な確認として記載します
2. **旧goalsテーブルの完全削除**: 読み取りコードは全て撤去済みですが、テーブル定義とバックアップ対象には残っています(後方互換)。将来のDexieバージョンアップ時にまとめて落とすならISSで

## SSOT

ISS-023/ISS-025対応済み・DEC-016を定常#7として記載
