# Handoff: v1.0 Release — ISS-010 本番ホスト判定の修正

- 日付: 2026-07-10
- 担当: Engineer(Claude Code)
- 対応指示書: `docs/engineering/instructions/2026-07-10_v1-release_iss010.md`
- ブランチ: `claude/tetsu-tanren-rename-p25kvo`(PR作成済み)

## ⚠️【Eiichiアクション】正式ドメイン値が未伝達です

指示時に伝達されるはずの**Eiichi確認の正式ドメインが、プレースホルダーのまま届きませんでした**(「(確認した値)」という文字列のみ)。指示書記載の `tanren-lake.vercel.app` は「例」と明記されているため、推測での設定は行っていません。

1. **PRをマージ**(この時点で本番の常時警告バナーは消える=🔴事象は解消。警告機能は一時無効)
2. **正式ドメインを1行設定**(どちらか。約1分):
   - リポジトリの `.env.production` の `VITE_PROD_HOST=` に正式ドメインを記入してcommit&push、または
   - Vercelダッシュボード→Settings→Environment Variables に `VITE_PROD_HOST=<正式ドメイン>`(Production)を追加してRedeploy
3. 設定後、本番URLで警告が出ない・プレビューURLで警告が出ることを実機確認
4. リリースタグ: `git tag -a v1.0.0 -m "v1.0.0: 初回リリース" && git push origin v1.0.0`(手順はREADMEに記載)

## 対応内容

### 要件1: ハードコード廃止+環境変数化

- `src/utils/env.ts`: 本番ホストを `import.meta.env.VITE_PROD_HOST` から取得。**未設定時は判定が常にfalse=警告機能無効**(指示書どおり「偽陽性より無警告の方がマシ」)
- 警告バナーは「プレビューホスト判定」と「本番URLリンク」の両方が揃うときのみ表示

### 要件2: リポジトリのデフォルト値

- `.env.production` を新設(Viteが本番ビルドで自動読込)。**値は上記の理由で空のまま**、設定手順をコメントで明記。Vercel環境変数でも上書き可能

### 要件3: 判定ユニットテスト

- 値を明示引数で渡す形にし、以下を固定: 偽陽性ケース(本番ドメイン自身→警告なし)/ 偽陰性ケース(`*-git-*.vercel.app` 等のプレビュー→警告あり)/ **未設定→常に警告なし** / localhost・偽装ドメイン(`vercel.app.example.com`)→対象外

### 要件4: README追記

- 「本番URLとリリース手順」セクションを追加: 本番URLの定義(=VITE_PROD_HOST)、mainマージで自動デプロイ、`vX.Y.Z` 注釈付きタグ運用、切り戻し(Vercel Redeploy)
- あわせて `package.json` のversionを **1.0.0** に更新

## 受け入れ条件の検証結果

- [x] 本番ドメインで警告が出ない/プレビューURLでは出る — **テストで固定**(12ファイル98本green・+2本)。※実URLでの表示確認はドメイン投入後にEiichiアクション3で
- [x] mainへのPR作成 — 済み
- [x] state.md更新 — ISS-010対応済み(値の投入待ちを明記)、現在フェーズ「v1.0リリース済み・定常運転(ドッグフーディング駆動)」

## 決定事項

1. 正式ドメイン未伝達のため、デフォルト値は**空=警告無効**で出荷(要件1のフォールバック仕様をそのまま適用)。値の投入は上記アクション2の1行のみ
2. versionを1.0.0に更新(タグ`v1.0.0`はEiichiのマージ後に打つ運用とした)
